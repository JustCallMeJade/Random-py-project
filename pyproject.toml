[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "turnip-builder"
version = "1.0.0"
description = "Lazy me"
requires-python = ">=3.10"

[project.scripts]
build-now = "turnip.main:main"
